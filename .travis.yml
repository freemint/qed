language: c
os: linux
dist: trusty
sudo: required

compiler:
   - m68k-atari-mint-gcc

before_install:
  - 'openssl aes-256-cbc -K $encrypted_a8c32f314d99_key -iv $encrypted_a8c32f314d99_iv -in .travis/deploy-key.enc -out .travis/deploy-key -d'
  - 'sh ./.install-cross-mint.sh'

script:
  - make CROSS=yes
